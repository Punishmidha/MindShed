<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-9">

        <div class="w-full flex justify-end mb-5">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm" href="/logout">Logout</a>
        </div>

        <h3 class="text-3xl mb-5">Welcome <span class="text-red-600">
                <%= user.name %>
            </span> to the profile page</h3>

        <h5 class="mb-5">You can create a new post here.</h5>

        <form class="mt-7 flex flex-wrap gap-1" action="/post" method="post">

            <textarea placeholder="What's on your mind"
                class=" p-3 outline-none w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                name="content" id=""></textarea>

            <input class="px-3 py-2 bg-blue-600 rounded-md" type="submit" value="Create">

        </form>

        <div class="posts mt-20">

            <h3 class="text-zinc-400">Your posts.</h3>

            <div class="postcontainer mt-3 ">

                <% posts.reverse().forEach(function (post) { %>

                    <div class="post w-1/3 rounded-md p-4 mb-4 border-2 border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2"> @<%= post.user.name %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>

                        <div class="btns flex mt-6 gap-6">

                            <div class="likes ">
                                <a class="" href="/like/<%= post._id %>">
                                    <%= post.likes.includes(user._id) ? "❤️" : "🤍" %>
                                </a>
                                <small class="">
                                    <%= post.likes.length %>
                                </small>
                            </div>


                            <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>

                            <a class="text-red-500 ml-auto  " href="/delete/<%= post._id %>"
                                onclick="return confirm('Are you sure you want to delete this post?')">Delete</a>


                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>
</body>

</html>