<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-9">
        <div class="w-full flex justify-end mb-5"> <a class="bg-red-500 rounded-md px-3 py-2 text-sm" href="/logout">Logout</a> </div>
        <h5 class="mb-5 text-lg">edit your post <span class="text-blue-500"> <%=post.user.username %> </span></h5>
        
        <form class="mt-7 flex flex-wrap gap-1" action="/update/<%=post._id%>" method="post">
            <!-- <input type="hidden" name="postId" value="<%= post._id %>"> -->
            <textarea class=" p-3 outline-none w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""> <%=post.content%></textarea>
            <input class="px-3 py-2 bg-yellow-400 text-black" type="submit" value="Update">
        </form>


    </div>
</body>

</html>